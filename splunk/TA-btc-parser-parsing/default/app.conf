[install]
is_configured = true

[ui]
is_visible = false
label = btc_parser - field/sourcetype config

[launcher]
author = btc_parser_app
description = props.conf (sourcetypes, timestamp/field extraction) for btc_parser_app's export/ CSVs. Deploy on the parsing tier (Splunk Cloud self-service app, or Enterprise indexers) - see docs/10-splunk-integration-und-feldkonzept.md.
version = 1.0.0
